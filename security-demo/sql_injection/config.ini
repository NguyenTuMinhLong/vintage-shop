; ============================================
; SQL Injection Demo - Database Configuration
; Copy this to config.php and fill in your values
; ============================================

; MySQL Hostname (from InfinityFree Control Panel)
DB_HOST = sql313.epizy.com

; Database Name (created in MySQL Databases)
DB_NAME = epiz_XXXXXX_demo

; MySQL Username (usually same as DB name prefix)
DB_USER = epiz_XXXXXX

; MySQL Password
DB_PASS = your_password
